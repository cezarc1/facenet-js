export { FaceDetector } from './FaceDetector';

export type {
  Detection,
  EmbeddingRequest,
  EmbeddingResult,
  FaceDetection,
  FaceDetectionDevice,
  FaceDetectionMode,
  FaceDetectionOptions,
  FaceDetectorState,
  FaceSimilarityResult,
  WasmFileset,
} from './types';
